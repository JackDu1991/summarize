# tools/apicheck

位置：`build/tools/apicheck`

概述：
- 作用：用于构建期校验的 API 检查相关工具。
- 主要文件：`Android.bp`、`etc/`。

编译方式：
- 由 `Android.bp` 管理的宿主工具/脚本包。
- 通常在校验或文档生成流程中使用。

相关构建文件：
- `Android.bp` 是入口。
- `etc/` 包含辅助数据或脚本。
