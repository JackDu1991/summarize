# tools/fs_get_stats

位置：`build/tools/fs_get_stats`

概述：
- 作用：输出文件系统统计信息的小型宿主工具。
- 主要文件：`Android.bp`、`fs_get_stats.c`。

编译方式：
- 由 `fs_get_stats.c` 编译得到的宿主 C 二进制。
- 供构建或打包诊断使用。

相关构建文件：
- `Android.bp` 定义宿主模块。
